// Code generated by windranger, DO NOT EDIT.
syntax = "proto3";
package authenticatorNested;

option go_package = "github.com/wzyjerry/auth/internal/ent/schema/authenticatorNested";

// 认证器类型
enum Kind {
    KIND_UNSET = 0; // 保留字：未设置
    KIND_ACCOUNT = 1; // 用户账户
    KIND_EMAIL = 2; // 邮箱
    KIND_PHONE = 3; // 手机
    KIND_CLEAR = -1; // 保留字：清空
}

// 用户账户认证器
message Account {
    optional string username = 1; // 账户名(*)
}

// 锚
message Anchor {
    optional Account account = 1; // 用户账户认证器
    optional Email email = 2; // 邮箱认证器
    optional Phone phone = 3; // 手机认证器
}

// 认证器
message Authenticator {
    optional string id = 1; // 主键
    optional string user_id = 2; // 用户ID
    optional Kind kind = 3; // 认证器类型
    optional Anchor anchor = 4; // 锚
}

// 邮箱认证器
message Email {
    optional string email = 1; // 邮箱(*)
}

// 手机认证器
message Phone {
    optional string phone = 1; // 手机号(*)
}
